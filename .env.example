# Server
PORT=4000
ALLOWED_ORIGIN=http://localhost:4000
KICK_BASE_URL=https://kick.com
# Optional: copy cookies from an authenticated browser session if Cloudflare blocks requests
KICK_COOKIES=
# Optional: force mock Kick response (ebonivon) for local testing
KICK_USE_MOCK=0
